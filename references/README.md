# References

`references.bib` is the shared bibliography for the collection.

Use stable BibTeX keys of the form `authorYYYYkeyword` when practical. Check DOI, journal metadata, edition, publisher, year, and page information against the primary source before adding an entry.
